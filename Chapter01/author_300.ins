INSERT INTO author (
       id,
       name,
       birth_date,
       gender )
SELECT 300,
       'Hugh Darwen',
       NULL,
       'MALE'
from   dual d
where not exists (
  SELECT 1
  FROM   author x
  WHERE  x.id = '300' );
 
COMMIT;
